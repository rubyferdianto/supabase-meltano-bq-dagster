austin_bikeshare_demo:
  outputs:
    dev:
      dataset: austin_bikeshare_demo
      job_execution_timeout_seconds: 300
      job_retries: 1
      location: US
      method: oauth
      priority: interactive
      project: infinite-byte-458600-a8
      threads: 1
      type: bigquery
  target: dev
bec_dbt:
  outputs:
    dev:
      dataset: "{{ env_var('TARGET_STAGING_DATASET', 'olist_data_staging') }}"
      job_execution_timeout_seconds: 300
      job_retries: 1
      method: service-account
      project: "{{ env_var('BQ_PROJECT_ID') }}"
      keyfile: "/Applications/RF/NTU/SCTP in DSAI/supabase-meltano-bq-dagster/bec_dbt/service-account-key.json"
      location: US
      priority: interactive
      threads: 5
      type: bigquery
  target: dev
resale_flat:
  outputs:
    dev:
      dataset: resale
      job_execution_timeout_seconds: 300
      job_retries: 1
      location: US
      method: oauth
      priority: interactive
      project: infinite-byte-458600-a8
      threads: 5
      type: bigquery
  target: dev
